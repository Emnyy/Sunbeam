<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Sunbeam.NotesMenuPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Sunbeam"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <x:Double x:Key="SettingsCardSpacing">4</x:Double>

        <Style x:Key="SettingsSectionHeaderTextBlockStyle"
           BasedOn="{StaticResource BodyStrongTextBlockStyle}"
           TargetType="TextBlock">
            <Style.Setters>
                <Setter Property="Margin" Value="1,30,0,6" />
            </Style.Setters>
        </Style>
    </Page.Resources>
    
    <StackPanel>
        <StackPanel VerticalAlignment="Top" x:Name="TopBar">
        <Grid>
            <Border  x:Name="NoteListButton" Padding="12" PointerPressed="ReturnToNotes" HorizontalAlignment="Left">
                <FontIcon Glyph="&#xE72B;" FontSize="24" Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"/>
            </Border>

            <Border Padding="12" HorizontalAlignment="Center">
                <AutoSuggestBox x:Name="Search" TextChanged="AutoSuggestBox_TextChanged" PlaceholderText="Search notes" QueryIcon="Find"/>
            </Border>

            <Border x:Name="Favorites" Padding="12" PointerPressed="ToggleFavortie" HorizontalAlignment="Right">
                <FontIcon Glyph="&#xE734;" FontSize="24" Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"/>
            </Border>
        </Grid>
    </StackPanel>
    <ScrollViewer>
            <StackPanel x:Name="NoteList" Padding="36, 0, 36, 12">
                
            </StackPanel>
    </ScrollViewer>
    </StackPanel>
</Page>