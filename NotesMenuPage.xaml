<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Sunbeam.NotesMenuPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Sunbeam"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource SystemAccentColor}">
        <StackPanel VerticalAlignment="Top" x:Name="TopBar">
            <Grid>
                <Border  x:Name="NoteList" Padding="12" PointerPressed="ReturnToNotes" HorizontalAlignment="Left">
                    <FontIcon Glyph="&#xE72B;" FontSize="24" Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"/>
                </Border>

                <Border Padding="12" HorizontalAlignment="Center">
                    <CalendarDatePicker PlaceholderText="Pick a date" Foreground="{ThemeResource SystemAccentColor}" Background="{ThemeResource TextOnAccentFillColorPrimaryBrush}"/>
                </Border>

                <Border x:Name="Favorites" Padding="12" PointerPressed="ToggleFavortie" HorizontalAlignment="Right">
                    <FontIcon Glyph="&#xE734;" FontSize="24" Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"/>
                </Border>
            </Grid>
        </StackPanel>
    </Grid>
</Page>
