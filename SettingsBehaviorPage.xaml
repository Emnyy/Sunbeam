<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Sunbeam.SettingsBehaviorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Sunbeam"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:ui="using:CommunityToolkit.WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <!--  These styles can be referenced to create a consistent SettingsPage layout  -->

        <!--  Spacing between cards  -->
        <x:Double x:Key="SettingsCardSpacing">4</x:Double>

        <!--  Style (inc. the correct spacing) of a section header  -->
        <Style x:Key="SettingsSectionHeaderTextBlockStyle"
           BasedOn="{StaticResource BodyStrongTextBlockStyle}"
           TargetType="TextBlock">
            <Style.Setters>
                <Setter Property="Margin" Value="1,30,0,6" />
            </Style.Setters>
        </Style>
    </Page.Resources>

    <ScrollViewer>
        <StackPanel>
            <TextBlock Text="Behavior" FontSize="40" FontWeight="SemiBold"  Padding="36, 40, 0, 36"/>

            <StackPanel Padding="36, 0, 36, 12">
                <controls:SettingsCard Margin="0, 0, 0, 4" Description="Fit text within window by default"
   Header="Word wrap"
   HeaderIcon="{ui:FontIcon Glyph=&#xE8C4;}">
                    <ToggleSwitch IsOn="True" />
                </controls:SettingsCard>

                <controls:SettingsExpander Margin="0, 0, 0, 4"
       Header="When Sunbeam starts"
       HeaderIcon="{ui:FontIcon Glyph=&#xE793;}">

                    <controls:SettingsExpander.Items>
                        <controls:SettingsCard HorizontalAlignment="Left" Background="Transparent">
                            <RadioButtons>
                                <RadioButton Content="Option 1"/>
                                <RadioButton Content="Option 2"/>
                            </RadioButtons>
                        </controls:SettingsCard>

                    </controls:SettingsExpander.Items>
                </controls:SettingsExpander>

            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
