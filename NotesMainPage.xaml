<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Sunbeam.NotesMainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Sunbeam"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{x:Bind GlobalItems}">

    <Grid x:Name="WinArea">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" VerticalAlignment="Top" x:Name="TopBar">
            <Grid Padding="0, 0, 0, -12">
                <Border x:Name="NoteListButton" Padding="12" PointerPressed="ListNotes"  HorizontalAlignment="Left">
                    <FontIcon Glyph="&#xEA37;" FontSize="24" Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"/>
                </Border>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <FontIcon x:Name="IsFavoriteStar1" Glyph="&#xE734;"  FontSize="24" Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"/>
                        <Border Padding="12" HorizontalAlignment="Center">
                        <TextBox x:Name="FileName" Text="{x:Bind GlobalMemory.CurrentFileFriendly, Mode=TwoWay}" FontSize="24" Background="{ThemeResource SystemAccentColor}" Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}">
                            <TextBox.Resources>
                                <SolidColorBrush x:Key="TextControlBackground" Color="{ThemeResource SystemAccentColor}"/>
                                <SolidColorBrush x:Key="TextControlBackgroundPointerOver" Color="{ThemeResource SystemAccentColorLight3}"/>
                                <SolidColorBrush x:Key="TextControlForegroundPointerOver" Color="{ThemeResource SystemAccentColor}"/>
                                <SolidColorBrush x:Key="TextControlBackgroundFocused" Color="{ThemeResource SystemAccentColorLight3}"/>
                                <SolidColorBrush x:Key="TextControlForegroundFocused" Color="{ThemeResource SystemAccentColor}"/>
                            </TextBox.Resources>
                        </TextBox>
                    </Border>
                    <FontIcon x:Name="IsFavoriteStar2" Glyph="&#xE734;" FontSize="24" Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"/>
                </StackPanel>

                <Border x:Name="Exit" Padding="12" HorizontalAlignment="Right">
                    <FontIcon Glyph="&#xE711;" FontSize="28" Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"/>
                </Border>
            </Grid>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Margin="10">
            <TextBox Text="{x:Bind GlobalMemory.CurrentFileContent, Mode=TwoWay}" FontSize="{x:Bind Settings.FontSize, Mode=TwoWay}" CanDrag="True" Foreground="{ThemeResource SystemAccentColor}" AcceptsReturn="True"  IsSpellCheckEnabled="True" x:Name="MainTextArea" TextWrapping="{x:Bind GlobalMemory.WrapFix}">
                <TextBox.Resources>
                    <SolidColorBrush x:Key="TextControlBackground" Color="{ThemeResource SystemAccentColorLight3}"/>
                    <SolidColorBrush x:Key="TextControlBackgroundPointerOver" Color="{ThemeResource SystemAccentColorLight3}"/>
                    <SolidColorBrush x:Key="TextControlBackgroundFocused" Color="{ThemeResource SystemAccentColorLight3}"/>
                    <SolidColorBrush x:Key="TextControlForegroundPointerOver" Color="{ThemeResource SystemAccentColor}"/>
                    <SolidColorBrush x:Key="TextControlForegroundFocused" Color="{ThemeResource SystemAccentColor}"/>
                </TextBox.Resources>
            </TextBox>
        </ScrollViewer>
    </Grid>
</Page>
