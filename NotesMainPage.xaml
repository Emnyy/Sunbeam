<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Sunbeam.NotesMainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Sunbeam"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource SystemAccentColor}" x:Name="WinArea">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" VerticalAlignment="Top" x:Name="TopBar">
            <Grid Padding="0, 0, 0, -12">
                <Border Padding="12" PointerPressed="ListNotes"  PointerEntered="NoteHover" PointerExited="NoteHoverStop" HorizontalAlignment="Left">
                    <FontIcon x:Name="NoteList" Glyph="&#xEA37;" FontSize="24" Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"/>
                </Border>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <FontIcon x:Name="IsFavoriteStar1" Glyph="&#xE734;"  FontSize="24" Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"/>
                    <Border Padding="12" HorizontalAlignment="Center" PointerEntered="TitleHover" PointerExited="TitleHoverStop">
                        <TextBlock x:Name="FileName" Text="Unnamed" FontSize="24" Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"/>
                    </Border>
                    <FontIcon x:Name="IsFavoriteStar2" Glyph="&#xE734;"  FontSize="24" Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"/>
                </StackPanel>

                <Border Padding="12" PointerPressed="ExitApp" PointerEntered="NoteHover" PointerExited="NoteHoverStop" HorizontalAlignment="Right">
                    <FontIcon x:Name="Exit" Glyph="&#xE711;" FontSize="28" Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"/>
                </Border>
            </Grid>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollMode="Enabled" VerticalScrollBarVisibility="Auto" Margin="10">
            <TextBox FontSize="20" CanDrag="True" Foreground="{ThemeResource SystemAccentColor}" AcceptsReturn="True"  IsSpellCheckEnabled="True" x:Name="MainTextArea" TextWrapping="Wrap">
                <TextBox.Resources>
                    <SolidColorBrush x:Key="TextControlBackground" Color="{ThemeResource SystemAccentColorLight3}"/>
                    <SolidColorBrush x:Key="TextControlBackgroundPointerOver" Color="{ThemeResource SystemAccentColorLight3}"/>
                    <SolidColorBrush x:Key="TextControlBackgroundFocused" Color="{ThemeResource SystemAccentColorLight3}"/>
                    <SolidColorBrush x:Key="TextControlForegroundPointerOver" Color="{ThemeResource SystemAccentColor}"/>
                    <SolidColorBrush x:Key="TextControlForegroundFocused" Color="{ThemeResource SystemAccentColor}"/>
                </TextBox.Resources>
            </TextBox>
        </ScrollViewer>
    </Grid>
</Page>
